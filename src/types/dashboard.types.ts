export interface IStatCardProps {
  Icon: React.ElementType;
  iconColor: string;
  metric: number;
  label: string;
}

export interface ProviderDashboardStatProps {
  title: string;
  Icon: React.ElementType;
  metric: number;
}
